import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* All project images are local files under /public/portfolio/ — no remote image sources configured. */
};

export default nextConfig;
